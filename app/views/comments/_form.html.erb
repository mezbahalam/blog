
<br />
<br />
<%= form_for([@post, @post.comments.build]) do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %><br>
	<br>
	<%= f.label :comment %>
	<%= f.text_area :body %><br>
	<br>
	<%= f.submit class: 'btn btn-success' %>
    <br>
    <br>
    <br>
<% end %>